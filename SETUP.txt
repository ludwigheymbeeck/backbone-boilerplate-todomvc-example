Run following commands in order to setup bower_components and node_modules directories:

# Install NPM dependencies.
npm install

# Install Bower dependencies.
bower install

Next, run grunt to minimize, uglify, and optimize the todo app:

grunt